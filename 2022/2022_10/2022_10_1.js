var input = `addx 15
addx -11
addx 6
addx -3
addx 5
addx -1
addx -8
addx 13
addx 4
noop
addx -1
addx 5
addx -1
addx 5
addx -1
addx 5
addx -1
addx 5
addx -1
addx -35
addx 1
addx 24
addx -19
addx 1
addx 16
addx -11
noop
noop
addx 21
addx -15
noop
noop
addx -3
addx 9
addx 1
addx -3
addx 8
addx 1
addx 5
noop
noop
noop
noop
noop
addx -36
noop
addx 1
addx 7
noop
noop
noop
addx 2
addx 6
noop
noop
noop
noop
noop
addx 1
noop
noop
addx 7
addx 1
noop
addx -13
addx 13
addx 7
noop
addx 1
addx -33
noop
noop
noop
addx 2
noop
noop
noop
addx 8
noop
addx -1
addx 2
addx 1
noop
addx 17
addx -9
addx 1
addx 1
addx -3
addx 11
noop
noop
addx 1
noop
addx 1
noop
noop
addx -13
addx -19
addx 1
addx 3
addx 26
addx -30
addx 12
addx -1
addx 3
addx 1
noop
noop
noop
addx -9
addx 18
addx 1
addx 2
noop
noop
addx 9
noop
noop
noop
addx -1
addx 2
addx -37
addx 1
addx 3
noop
addx 15
addx -21
addx 22
addx -6
addx 1
noop
addx 2
addx 1
noop
addx -10
noop
noop
addx 20
addx 1
addx 2
addx 2
addx -6
addx -11
noop
noop
noop`;

function isTime(cycle) {
  if (cycle == 20) {
    return true;
  }

  var blah = cycle - 20;
  if (blah % 40 == 0) {
    return true;
  }
  
  return false;
}

var cycle = 0;
var x = 1;
var sum = 0;
var lines = input.split('\n');
lines.forEach(line => {
  if (line == 'noop') {
    cycle++;
    if (isTime(cycle)) {
      sum += cycle * parseInt(x);
    }
  } else if (line.startsWith('addx ')) {
    cycle++;
    if (isTime(cycle)) {
      sum += cycle * parseInt(x);
    }
    cycle++;
    if (isTime(cycle)) {
      sum += cycle * parseInt(x);
    }
    x += parseInt(line.split(' ')[1]);
  }
});

console.log(sum)